//Write a Java program to rotate the elements of an array to the right by a given number of positions. The program should take an array and the number of positions to rotate as input and output the resulting rotated array.

import java.util.*;
public class arraysum{
	 public static int sumArray(int[] arr){
		 int sum=0;
		 for (int num : arr) {
            sum += num;
        }
		return sum;
	 }
	public static void main(String[]args){
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter the size of the array");
		int size =sc.nextInt();
		int[] arr=new int [size];
		//int sum=0;
		int c=0;
		
		System.out.println("Enter the array elements");
		for(int i=0;i<size;i++)
		{
			arr[i]=sc.nextInt();
		}
		int sum=sumArray(arr);
		System.out.println("The sum of number is :"+sum); 
	}
}